# dirs
.pnpm-store/
deploy/local/authelia/certs/
node_modules/
worktrees/
dist/

# files
status.md
# built binary (not the api/server/ proxy package directory)
/api/server/server
*.tsbuildinfo
.DS_Store
.env
CLAUDE.md
!.claude/CLAUDE.md
# this gets generated based on plugins
/.mcp.json

# Worktree-local go.work files for isolated builds. Gitignored — no
# aggregate-root go.work is committed; each worktree gets its own local copy
# (see docs-mf-standards/building-modules.md).
/go.work
/go.work.sum

# OAuth/OIDC credential files that must never be committed (see
# plan/followups.yaml history: pDQQ — live secrets were found committed
# at the repo root in commit 9680136 and purged from git history).
client_secret_*.apps.googleusercontent.com.json
ms-secret-*.token
