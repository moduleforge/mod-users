# model

Postgres 16 schema, Atlas declarative migrations, and sqlc-generated Go queries
for the users-module.

- `migrations/` — Atlas migration files (populated in Phase 2).
- `queries/` — sqlc query files (populated in Phase 2).

Placeholder; no schema has been authored yet.
